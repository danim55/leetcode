class Solution {
    public List<List<Integer>> generate(int numRows) {
        List<List<Integer>> pascalTrianble = new ArrayList<>();
        for (int = 0; i < 30; i ++) {
            List<Integer> auxiliarList = new ArrayList<>();
            auxiliarList.set(0, 1)
            auxiliarList.set(auxiliarList.size(), 1) ;
            for (int j = 1; j < i; j++) {
                auxiliarList = new ArrayList<>(i+1);
                auxiliarList
            }
        }
        return pascalTriangle;
    }
}